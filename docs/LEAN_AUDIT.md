# Audit Lean

La version canonique est déplacée dans :

```text
AGENTS/LEAN_AUDIT.md
```

Ce fichier `docs/LEAN_AUDIT.md` reste comme pointeur stable pour les liens anciens.

Commande courte recommandée :

```bash
make build-log-all && make audit-sorries && make audit-warnings && make doctrine-check
```
